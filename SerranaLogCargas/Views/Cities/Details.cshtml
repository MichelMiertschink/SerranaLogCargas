@model SerranaLogCargas.Models.City

@{
    ViewData["Title"] = "Cidade";
}

<h1>Detalhes</h1>
<h4>Cidade</h4>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.State.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.State.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-secondary">Editar</a> |
    <a asp-action="Index" class="btn">Retornar</a>
</div>